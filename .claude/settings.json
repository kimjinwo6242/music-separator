{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "Set-Location 'C:\\Users\\user\\music-separator'; $changes = git status --porcelain; if ($changes) { $ts = Get-Date -Format 'yyyy-MM-dd HH:mm'; git add .; git commit -m \"auto: Claude Code changes ($ts)\"; git push }",
            "timeout": 30,
            "statusMessage": "GitHub에 저장 중..."
          }
        ]
      }
    ]
  }
}
